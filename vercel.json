{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "installCommand": "npm install -g pnpm@10.27.0 && cd frontend && pnpm install",
  "buildCommand": "cd frontend && pnpm run build"
}
