{
  "framework": "vite",
  "installCommand": "cd frontend && npm install -g pnpm@10.27.0 && pnpm install",
  "buildCommand": "cd frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
