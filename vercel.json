{
  "installCommand": "corepack enable && corepack prepare pnpm@10.27.0 --activate && cd frontend && pnpm install",
  "buildCommand": "cd frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/((?!assets/|.*\\.).*)", "destination": "/index.html" }
  ]
}
